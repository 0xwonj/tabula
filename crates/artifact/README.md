# tabula-artifact

Canonical artifact models shared across Tabula adapters and orchestration.

## Role

- Defines common program/state/batch JSON models.
- Defines the canonical execution receipt model.
- Provides conversion helpers (state cells, tx sender parsing).
- Provides deterministic state merge helper used by execute/prove flows.
- Provides generic JSON load/write helpers.
